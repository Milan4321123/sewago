# The deployed SewaGo backend the app loads inside its WebView.
# For a real installable APK this MUST be a public https URL (not localhost / LAN IP),
# otherwise the app only works while you are on the same wifi as your computer.
EXPO_PUBLIC_SEWAGO_URL=https://your-deployed-sewago-domain.example
